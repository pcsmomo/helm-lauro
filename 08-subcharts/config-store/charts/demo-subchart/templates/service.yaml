apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-subchart.fullname" . }}
  labels:
    {{- include "demo-subchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo-subchart.selectorLabels" . | nindent 4 }}
